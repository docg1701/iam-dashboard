{
  "name": "@iam-dashboard/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types and utilities for IAM Dashboard",
  "main": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}